<UserControl x:Class="XinSpect.HostsEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:XinSpect"
             FontFamily="{StaticResource AppFont}">
    <Grid Margin="0,0,6,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="2,0,0,10">
            <local:SectionHead Text="Hosts 編輯器" HelpKey="hosts/Hosts 編輯器"/>
            <TextBlock Style="{StaticResource FieldLabel}" Margin="0,4,0,0" TextWrapping="Wrap"
                       Text="編輯 Windows hosts 檔（網域→IP 對應）。儲存前自動備份，可一鍵還原上一版並清除 DNS 快取。需以系統管理員身分執行方能寫入。"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,12" Padding="14,10">
            <StackPanel Orientation="Horizontal">
                <Button Style="{StaticResource PrimaryButton}" Content="儲存" Click="Save_Click"/>
                <Button Style="{StaticResource ToolButton}" Content="重新載入" Click="Reload_Click" Margin="10,0,0,0"/>
                <Button Style="{StaticResource ToolButton}" Content="還原上一版備份" Click="Restore_Click" Margin="10,0,0,0"/>
                <Button Style="{StaticResource ToolButton}" Content="清除 DNS 快取" Click="Flush_Click" Margin="10,0,0,0"/>
                <Button Style="{StaticResource ToolButton}" Content="開啟所在資料夾" Click="OpenFolder_Click" Margin="10,0,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0" Margin="0,0,0,4">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBox x:Name="Editor" Grid.Row="0" AcceptsReturn="True" AcceptsTab="True"
                         FontFamily="Consolas" FontSize="13" TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                         Background="Transparent" BorderThickness="0" Padding="14,12"
                         Foreground="{DynamicResource PrimaryInkBrush}" CaretBrush="{DynamicResource AccentBrush}"/>
                <Border Grid.Row="1" Background="{DynamicResource Surface2Brush}" Padding="12,6" CornerRadius="0,0,10,10">
                    <TextBlock x:Name="StatusText" FontSize="11.5" Foreground="{DynamicResource SecondaryInkBrush}"/>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
