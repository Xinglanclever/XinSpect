# 曦覽 XinSpect ・ 交接說明（2026-08-30）

> **由 Claude Opus 4.8 於 2026-08-30 清晨撰寫**，交接自 GLM 5.3 Flash（其可用 token 將盡）。
> 前一份 `HANDOFF-1.3.2.md` 已完成使命（v1.3.2 已發佈），**本文取代它**。
> 長期方向清單仍看 `ROADMAP-給GLM.md`（第一到第八部，第四～七輪彙整都在裡面）。
> 三份文件皆未納入版控。

## 一、最重要的一句話

**工作樹目前編譯不過。** 接手的第一件事是修好它，不是開新功能。
GLM Flash 是在實作途中斷線的：`Views/BenchView.xaml` 與 `Views/BenchView.xaml.cs`
按專案慣例（`CacheBenchService` 形狀）寫好了 UI，但 `Services/TopDownService.cs`
寫成了另一種形狀（`Refresh()`／`IsLoading`／`Rows`），兩邊對不上。

## 二、git 狀態

- 分支 `main`，HEAD = `2a11872`（RDT 逐核心 L3 占用與記憶體頻寬）。
- **HEAD 與遠端同步，沒有未 push 的提交。**
- 未提交的工作（全部屬於「第六輪三優先」的 Top-down 項與記憶體真實面貌）：

| 狀態 | 檔案 |
|---|---|
| 修改 | `Services/RdtService.cs`（+18/−3，看起來正確，見第四節） |
| 修改 | `ViewModels/MainViewModel.cs`（+9：`Rdt`／`TopDown`／`MemoryTruth` 三個服務屬性） |
| 修改 | `Views/BenchView.xaml`(+36) `/.xaml.cs`(+5)、`Views/CpuView.xaml`(+40) `/.xaml.cs`(+19)、`Views/MemoryView.xaml`(+32) |
| 新增 | `Services/TopDownService.cs`(212)、`Services/MemoryTruthService.cs`(79)、`Tests/TopDownTests.cs`(53) |
| 新增（未版控文件） | `HANDOFF-1.3.2.md`、`ROADMAP-給GLM.md`、本檔 |

## 三、已驗證的缺陷（`dotnet test` 實跑，不是推測）

### 1. 編譯錯誤 CS1061 ×2 — `Views/BenchView.xaml.cs:124-125`

code-behind 呼叫 `TopDown?.Start()` 與 `TopDown?.Cancel()`，`TopDownService` 沒有這兩個方法。
`BenchView.xaml` 另外綁了四個不存在的成員：`TopDown.IsRunning`、`TopDown.CanStart`、
`TopDown.ProgressPercent`、`TopDown.Buckets`（`Buckets` 的 DataTemplate 用 `Name`／`Note`／`Percent`）。

**建議修法：改服務去符合 XAML，不要改 XAML。** XAML 寫的正是 `Services/CacheBenchService.cs`
的專案慣例形狀（`IsRunning`/`CanStart`/`Status`/`ProgressPercent`/`ObservableCollection`／
`Start()` → `_ = RunAsync()`／`Cancel()` → `_cts?.Cancel()`），服務那邊才是偏離慣例的一方。
現有的 `TopDownRow`（逐邏輯處理器四桶）與 XAML 期待的 `Buckets`（四個桶各一列，含說明文字）
是**兩種不同的呈現**——需要決定留哪一個，或兩者都給（全系統四桶條圖 ＋ 逐核明細表）。
XAML 目前只畫得出前者。

### 2. 無號數下溢 — `Services/TopDownService.cs:207`

```csharp
double badSpec = Math.Clamp((iss - ret) * 100.0 / slots, 0, 100);
```

`iss`／`ret` 都是 `ulong`。`ret > iss` 時（逐核讀取有時間差，實務上會發生）
`iss - ret` 繞回接近 2^64，乘完夾成 100%，Backend 被擠成 0——**四桶歸因整列失真**。
修法：先轉 `double` 再相減，例如 `((double)iss - ret) * 100.0 / slots`。

`Tests/TopDownTests.cs:39` 的 `負值BadSpec夾為零` 正好蓋到這個案例
（`Compute(100, 500, 100, 0, 0)` 期望 `bs == 0`，實際會得 100）。**那支測試從未被執行過**，
因為專案在它之前就編譯失敗了。修好第 1 點之後它會立刻紅，屬預期。

另外 `Compute` 的 `rec`（`INT_MISC.RECOVERY_CYCLES`，PMC3）**收了參數但完全沒用**。
Intel v1 的 Bad Speculation 標準式含 recovery 項；XAML 的說明文字已誠實寫出
「Bad Speculation 不含 RECOVERY 項（4 計數器限制）」——要嘛用它，要嘛把 PMC3 的設定拿掉，
不要留一個白寫的計數器。

### 3. `MemoryTruthService` 的繫結是死的 — `Services/MemoryTruthService.cs:33-39`

繼承 `ObservableObject`，但五個對外屬性全是 `{ get; private set; }` 自動屬性，
**沒有一個走 `SetProperty`**，所以 `Refresh()` 改完值 UI 不會更新。
更根本的是：**全專案沒有任何地方呼叫 `MemoryTruth.Refresh()`**
（`Views/MemoryView.xaml.cs` 未被修改，也沒有計時器）。
`Views/MemoryView.xaml:284-316` 那張「記憶體真實面貌（認可）」卡會永遠顯示
「—／—／—／尚未讀取。」

修法：屬性改 `SetProperty` 後端欄位；在 `MemoryView` 的 code-behind 掛
`Loaded` 或 `DispatcherTimer`（`CpuView` 的 RDT 計時器可作範本，見 `Views/CpuView.xaml.cs:11-17`）。
`MemoryTruthService.Judge` 是純函式但**目前沒有測試覆蓋**——照專案慣例應該補一支。

### 4. 警告 CS0169 — `Services/RdtService.cs:55`

`_maxRmid` 從未使用。清掉或用起來（`CPUID 0x0F` 的 max RMID 本來該拿來驗證 RMID 指派範圍）。

## 四、關於 `IA32_L3_QOS_CFG`（0xC81）的更正

本節原先寫「全核寫 0xC81 bit0 以啟用 L3 QoS 監測」是對的——**那是錯的，已隨 GLM 的撤銷一併消失，不要復原**。
0xC81 bit0 是 L3 **CAT** 的 CDP（Code/Data Prioritization）啟用位元，屬**分配**（allocation）機制；
CMT／MBM **監測**不需要任何全域啟用位元，只需要 PQR_ASSOC 指派 RMID＋QM_EVTSEL 選事件。
「Intel PCM 亦全核寫」這句沒有依據。寫 0xC81 只會改動快取分配設定，不會讓監測計數動起來。

`QM_CTR` 位 63／62 檢查與親和性 try/finally 還原這兩點方向是對的，已由我實作（見第八節）。

## 五、本機 RDT 實測結論（2026-08-30，親和性 bug 修好之後重測）

HEAD 提交訊息記載「本機 RDT 計數恆為 0」時，PQR_ASSOC 是在**未釘選核心**的情況下寫的，
等於只有一顆核心真的拿到 RMID——那份測量不足以下結論。修好之後我用一次性探針重測（用完即刪）：

```
CPUID 0F.0 EBX=0x8F (bit0 CMT=True)
CPUID 0F.1 EBX(upscaling)=73728  ECX(maxRmid)=143  EDX(events)=0x7
processMask=0xFFFFFFFFF lps=36
lp0..lp3: writePQR=True readback=0x100000000     ← RMID 欄位（位 51:32）＝1，寫入確實落地
round0 lp0..lp3: occ=0 total=0 local=0
round1 lp0..lp3: occ=0 total=0 local=0           ← 相隔一秒，MBM 完全沒有累積
done, RMID reset to 0
```

三個事件都被 CPUID 宣告支援（EDX=0x7），`QM_CTR` 沒有回 Error（位 63）也沒有回 Unavailable（位 62），
但值恆為 0 且不遞增。**結論不變：這台平台／韌體沒有開放 RDT 監測；顯示 0 是誠實的結果，不是 bug，
不要為了「讓數字動起來」去補值或去寫 0xC81。** 差別在於現在這個結論是在指派正確的前提下得到的。

## 六、驗證指令（使用者的 XinSpect.exe 常在執行中，會鎖輸出）

```bash
dotnet test Tests/XinSpect.Tests.csproj -v q --nologo
```

測試會連帶編譯主專案（等於驗證 XAML），且不受 apphost 檔案鎖影響——**這是首選的驗證方式**。
單純建置請用 `dotnet build XinSpect.csproj -c Debug -v q --nologo -p:BaseOutputPath=obj/_verify/`；
發佈用 `-p:BaseOutputPath=obj/_pub/ -o publish`。**不要 taskkill 使用者正在跑的程式。**

測試基準數：1.3.2 發佈時為 **434 項全通過**。`Tests/TopDownTests.cs` 新增 5 項，
修好之後應為 439 項（其中 `負值BadSpec夾為零` 需第 2 點的修正才會綠）。

## 七、版本節奏

1.3.2 已發佈（`v1.3.2`，附 `XinSpect.exe`）。使用者原話：
**「可以，不過一次成型1.3.2而不是經常的版本迭代」**——第六輪這批做完再累積成下一版，
**不要每修一項就 bump 版號**。

## 八、其餘一切照舊

專案主軸（誠實）、地雷清單、慣例、使用者互動注意事項、這台機器的硬體事實
**全部見 `ROADMAP-給GLM.md`**（第五部「給 GLM 的執行守則」與第四部「誠實紅線彙總」是必讀）。
`HANDOFF-1.3.2.md` 第五、六、七、八節的地雷清單仍然有效，尚未併入 ROADMAP，先別刪那份檔。

**唯一要補進地雷清單的新項目：**
**寫完服務與 XAML 之後一定要跑一次 `dotnet test`。**
這次斷線留下的三個缺陷，兩個是編譯期就會抓到、一個是自己寫的測試就會抓到——
**全部是「沒跑驗證」造成的，不是難題。**

— Claude Opus 4.8，2026-08-30

---

## 九、已完成（2026-08-30 當日，由 Claude 接手後）

### 記憶體真實面貌（認可）— 第三節那一項，做完了

- `Services/MemoryTruthService.cs` 重寫。**除了繫結是死的以外還有一個更嚴重的、GLM 沒發現的錯**：
  `PERFORMANCE_INFORMATION` 少宣告了 `KernelTotal`，又把 `HandleCount`／`ProcessCount`／`ThreadCount`
  誤宣告成 `ulong`；兩個錯誤剛好抵銷成同樣的 104 bytes，於是 `cb` 相符、`GetPerformanceInfo` 回 TRUE，
  但 `SystemCache` 之後的每個欄位都位移了一格——`PageSize` 讀到的其實是 native 的 `KernelNonpaged`。
  **結構大小相符不代表結構正確。** 已改成正確的十個 `nuint` ＋ 三個 `uint`。
- 五個對外屬性改走 `SetProperty`；`MemoryView.xaml.cs` 加 `Loaded` ＋ 兩秒 `DispatcherTimer` ＋ `Unloaded` 停表。
- `MemoryView.xaml` 補一列「頁面大小」（量到的值，不是假設 4 KB）。
- 純函式抽成 `MemoryTruthMath.ToGigabytes`／`Judge`，`Tests/MemoryTruthMathTests.cs` 六支測試涵蓋
  （含頁面大小為 0 不除以零、尖峰恰等於實體不算超過兩個邊界）。
- 誠實用語收緊：原文寫「系統曾經真的動用分頁檔」，改成「曾有一部分認可只能由分頁檔支撐」，
  並明說本卡片**不宣稱**那些頁面真的被寫到磁碟。
- 執行期實測（獨立探針，已刪）：`SizeOf=104 ok=True cbBack=104`、
  `PageSize=4096 CommitGB=14.87 LimitGB=49.69 PeakGB=16.89 PhysGB=31.69`——31.69 GB 對得上這台 32 GB 的機器。

### `Services/RdtService.cs` — 四個缺陷修完

1. `_upscaling`／`_maxRmid` **原本從未被指派**（`DetectSupport` 只算了同名區域變數就丟掉），
   所以占用量一直是未縮放的原始值、`_maxRmid` 觸發 CS0169。現在都指派了，
   並用 `_maxRmid` 檢查 `UsedRmid` 是否落在合法範圍內（不足就如實說「最大 RMID 為 N」而非硬幹）。
2. **PQR_ASSOC 與 UnassignRmid 都沒有釘選核心**。MSR 寫入只作用於當下執行的那顆核心，
   原本的 `foreach (var lp in _lps) WriteMsrPair(...)` 等於對同一顆核心寫了 36 次。
   兩處都改成逐核心 `SetThreadAffinityMask` ＋ `try/finally` 還原 `_processMask`。
3. `QM_CTR` 的位 63（Error）／位 62（Unavailable）**沒有檢查**，錯誤碼會被當成讀值。
   抽出 `ReadQmCtr()` 統一處理：任一旗標成立回 `null`，資料只取位 61:0。
4. 頻寬換算用的 `* 8.0` 是沒有依據的魔術數字，改成 CPUID 0x0F.1 EBX 的 upscaling
   （本機 73728 bytes，與 Linux resctrl 的 `mon_scale` 同一個值），並改用 MB＝1024²。
   另外三個事件現在各自看 CPUID EDX 的能力位元，不再假設三者都有。

`DetectSupport` 也改成在 `Start()` 裡先取得核心清單再呼叫，說明文字才不會顯示「0 邏輯處理器」。

### 驗證

`dotnet build XinSpect.csproj` → 0 警告 0 錯誤（CS0169 消失）。
`dotnet test Tests/XinSpect.Tests.csproj -p:BaseOutputPath=obj/_testbin11/` → **495 全通過**
（GLM 收尾後的基準 489，加上我新增的 6 支記憶體換算測試）。

### 未動、留給後續

- `Services/CpuzReportService.cs:391-392` 仍在 TASK-GLM 的禁區清單裡，未碰。

— Claude Opus 4.8，2026-08-30（同日續）

---

## 十、Top-down Level 1 完成（2026-08-30，commit `996cb62`）

從零重寫，與 GLM 的版本無關（那份已在本日先前的整理中移除）。

### 為什麼可以做——先探再寫

`TraceEvent` 的 ETW profile-source 路徑表達不出 Top-down 需要的原始事件，所以走
既有的 `WinRing0Bridge`（MSR 讀寫都可用）。動手前用三支一次性探針（皆已刪除）確認：

1. PERFEVTSEL0-3 四個回讀值完全相符，計數合理（IPC 1.13、sqrt 迴圈 Retiring≈37%）。
2. AnyThread（位 21）**沒有被硬體剝除**：回讀 `0x63019C`，且 CLK(any) 840M > CLK(thread) 782M。
3. `GetLogicalProcessorInformationEx(RelationProcessorCore)` 的位移正確：本機 18 顆實體核心 × 2 LP。
   （payload 位移：flags +0、groupCount +22、GROUP_AFFINITY.Mask +24、Group +32。）

另外確認**沒有別人在用通用計數器**：存下來的 PERFEVTSEL0-3 全是 `0x0`，
`GLOBAL_CTRL=0x60000000F` / `FIXED_CTR_CTRL=0x220` 只是 Windows 正常用固定計數器算使用率。

### 兩個當初卡住的決定，我的答案

- **呈現形態：兩者都給。** 全系統四桶（含說明文字的長條）＋逐實體核心 18 列明細。
- **時鐘來源：不動固定計數器。** `FIXED_CTR1` 正被 Windows 用來算 CPU 使用率，
  歸零或改它的設定會擾動作業系統的統計；改用 `CPU_CLK_UNHALTED.THREAD_P`（0x3C/0x00）
  占掉一個通用計數器。剩下三格剛好放 `0x9C/01`、`0xC2/02`、`0x0E/01`——
  **第五個事件 `INT_MISC.RECOVERY_CYCLES` 無處可放，所以 Bad Speculation 不含 recovery 項。**
  這件事寫在卡片上，不假裝是完整公式（BadSpec 略低估、Backend 略高估）。
- **SMT：以 AnyThread 逐實體核心量。** `IDQ_UOPS_NOT_DELIVERED.CORE` 本質是核心層事件，
  按邏輯處理器歸因會重複計算；開 AnyThread 後四個事件都是整顆核心的量，分母 4×CLKS 才對得上。

### 順手修掉的死程式碼

`2a11872` 只提交了 `Services/RdtService.cs`，**全專案沒有任何地方引用它**——那張 RDT 卡片
在 UI 上根本不存在。現已補上 `Views/CpuView.xaml` 的卡片與 code-behind 的一秒
`DispatcherTimer`，並修掉 `RdtService.Tick` 少的一次 `OnPropertyChanged(nameof(Status))`
（背景執行緒只能寫 `_status` 欄位，原本狀態列永遠不更新）。

### 驗證

`dotnet build` 0 警告 0 錯誤；`dotnet test` **504 全通過**（495＋9 支 `TopDownMathTests`）。
實機端到端（臨時測試檔，跑完即刪）：Intel Family 6 Model 0x55、PMU 版本 4、
通用計數器 4 個 48 位元；18 顆實體核心全部取到值，閒置系統下
Retiring 18.2%／BadSpec 2.7%／Frontend 34.7%／Backend 44.3%，每周期退休插槽 0.73/4。
`obj/_pmuprobe`、`obj/_rdtprobe` 都已刪除。

**版號未動**（照使用者「一次成型」的節奏，累積到下一版再 bump），未 push。

— Claude Opus 4.8，2026-08-30（同日再續）
