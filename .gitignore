# ---- 建置產物 ----
[Bb]in/
[Oo]bj/
publish/
publish-*/
Bridge/[Bb]in/
Bridge/[Oo]bj/

# ---- Visual Studio / Rider ----
.vs/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates

# ---- NuGet ----
*.nupkg
*.snupkg
packages/
project.lock.json
project.assets.json

# ---- 執行階段 / 暫存 ----
*.log
*.tmp
*.cache

# ---- 專案內暫存檔（非原始碼）----
*.zip
cpuz_probe.txt
Bridge/dump2.txt
Bridge/s2.txt
Bridge/server_out.txt
Bridge/test_in.txt

# Bridge 的 SDK 探勘／除錯傾印
Bridge/*.txt

# wrangler 本機狀態（部署快取、暫存設定；不屬於原始碼）
cloudflare/**/.wrangler/

# ---- UI 驗證截圖（verify*.ps1 以 UI Automation 跑出來的產物）----
# 這些是開發過程的證據，不是原始碼；留在索引裡會被打進每個 tag 的 Source code (zip)，
# 而且畫面永遠停在拍攝當天的版本，對下載的人只會造成誤解。要看就在本機重跑 verify*.ps1。
shots/
shot1.png
win.png
crop_gauges.png
tab_*.png
